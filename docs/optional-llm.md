# Optional LLM notes

**English** · [简体中文](optional-llm.zh-CN.md)

Most users can **ignore** this.

Cursor / Claude (host agent = the model) only need `skills/` + `docs/workflow.md`. No `.env` required.

If you call an OpenAI-compatible HTTP API yourself (Ollama, gateways), see the leftover notes under [`ai-engine/`](../ai-engine/) (not installed into `~/.maker-flow` by default).
