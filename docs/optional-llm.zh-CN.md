# 可选 LLM 说明

[English](optional-llm.md) · **简体中文**

**多数用户可忽略。**

Cursor / Claude（宿主 Agent 即模型）只需 `skills/` + `docs/workflow.md`，无需 `.env`。

若自行调用 OpenAI 兼容 HTTP API（Ollama、网关等），见 [`ai-engine/`](../ai-engine/) 残留说明（默认不随 `maker-flow install` 装进 `~/.maker-flow`）。
