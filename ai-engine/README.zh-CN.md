[English](README.md) · **简体中文**

# ai-engine/（可选 — 通常不用）

**多数用户应忽略本目录。** 宿主 Agent（Cursor / Claude）不需要它。

见 [`docs/optional-llm.md`](../docs/optional-llm.md)。`maker-flow install` 默认不会拷进 `~/.maker-flow`。
