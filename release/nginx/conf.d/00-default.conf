# Default vhost so the gateway can start with an empty MVP set.
# MVP server blocks are added as conf.d/<MVP_NAME>.conf by push-and-route.sh.
server {
    listen 80 default_server;
    server_name _;
    return 200 "maker-flow gateway\n";
    add_header Content-Type text/plain;
}
